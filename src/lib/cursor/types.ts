// Project shim: re-exports the canonical types from the extracted kit so
// existing import paths (`@/lib/cursor/types`) keep working unchanged.
export * from "../../../packages/cursor-chat-kit/src/types";
